void mergesort(vector<int> &a , int low, int high){
           int mid=(low+high)/2;
           ms(a,low,mid);
           ms(a,mid+1;high);
           
}
void merge(vector<int> &a, int low,int mid, int high){
          
}